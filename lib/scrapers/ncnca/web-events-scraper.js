/* scrapes additional event information using web link, unfortunately as of today it's the nonFriendlyName
way to get "meta" fields since Calendar plugin NCNCA uses on wordpress blog does not support
meta fields in their REST API */
