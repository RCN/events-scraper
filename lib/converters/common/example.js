/*

  Convertion consist of few parts:
    - get events to convert
    - get previous RCN events
    - convert events
    - commit them to gihub if similar


  const convert = (previousRcnEvents, events) => ({ newRcnEvents })
  module.exports = convert
 */
